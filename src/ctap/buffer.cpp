#include <Arduino.h>

#include "ctap/buffer.h"

namespace CTAP
{
    InputBuffer inputBuffer;

    void InputBuffer::reset()
    {
    }

    void InputBuffer::setCmd(uint8_t cmd)
    {
    }

    void InputBuffer::setLength(uint16_t len)
    {
    }

    void InputBuffer::append(uint8_t *data, size_t len)
    {
    }

} // namespace CTAP